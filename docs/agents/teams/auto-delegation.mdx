---
sidebar_position: 7
---

# 자동 위임 시스템

GitHub Issue 기반 PM→Dev 자동 위임 시스템.

:::info 🎯 목적
PM이 Issue를 생성하면 Dev가 자동으로 작업을 시작합니다.
수동 개입 없이 스펙→구현→리포트까지 자동화.
:::

## 워크플로우

```mermaid
flowchart TB
    subgraph PM["1️⃣ PM"]
        SPEC[마이크로 PRD 작성]
        ISSUE[Issue 생성 + handoff:pm→dev]
    end

    subgraph Auto["2️⃣ 자동 알림"]
        DETECT[GitHub Actions 트리거]
        SLACK[Slack 알림]
        COMMENT[Issue 코멘트]
    end

    subgraph Manual["3️⃣ 수동 실행"]
        HUMAN[알림 확인]
        CLAUDE[Claude Code 세션 시작]
        DEVCYCLE[/dev-cycle 실행]
    end

    subgraph Dev["4️⃣ AI Dev"]
        IMPL[구현]
        TEST[테스트]
        PR[PR 생성]
    end

    SPEC --> ISSUE
    ISSUE --> DETECT
    DETECT --> SLACK
    DETECT --> COMMENT
    SLACK --> HUMAN
    HUMAN --> CLAUDE
    CLAUDE --> DEVCYCLE
    DEVCYCLE --> IMPL
    IMPL --> TEST
    TEST --> PR
```

## 설정 방법

### 1. 필수 Secrets 설정

GitHub 레포지토리 Settings → Secrets에 추가:

| Secret | 설명 |
|--------|------|
| `SLACK_WEBHOOK_URL` | Slack 웹훅 URL |

### 2. 라벨 설정

```bash
./scripts/setup-labels.sh org-tinysolver/ground-truth
```

생성되는 라벨:
- `handoff:pm→dev` - PM→Dev 위임 트리거
- `handoff:pm→research` - PM→Research 위임
- `team:dev`, `team:pm`, `team:research`
- `ready`, `blocked`, `human-review`, `all-hands`

## 사용 방법

### PM이 Dev에게 위임

```bash
gh issue create \
  --repo org-tinysolver/ground-truth \
  --title "Ground Truth 다크 모드 추가" \
  --label "team:dev,handoff:pm→dev,ready" \
  --body "## 요약
다크 모드 토글 기능 추가

## 컨텍스트
사용자 요청 다수

## 작업 내용
- [ ] 다크 모드 CSS 추가
- [ ] 토글 버튼 구현

## 완료 조건
다크/라이트 전환 동작"
```

### 실행 흐름

1. Issue 생성 시 `handoff:pm→dev` 라벨 감지
2. GitHub Actions 워크플로우 자동 시작
3. Slack에 작업 요청 알림
4. Issue에 작업 방법 코멘트
5. **수동:** Claude Code 세션에서 `/dev-cycle` 실행
6. 구현 및 PR 생성

## Slack 알림 예시

```
🎯 AI Dev 작업 요청
이슈: #123 Ground Truth 다크 모드 추가
요청자: pm-bot

작업 시작 방법:
gh issue view 123 --repo org-tinysolver/ground-truth

# Claude Code에서 /dev-cycle 실행

[📋 이슈 보기] [🚀 Claude Code 열기]
```

## 트리거 조건

| 라벨 | 트리거 대상 | 워크플로우 |
|------|------------|-----------|
| `handoff:pm→dev` | AI Dev | `ai-dev-dispatch.yml` |
| `handoff:pm→research` | AI Research | (예정) |
| `handoff:dev→pm` | AI PM | (예정) |

## 현재 방식

- **알림만 자동**: Slack 알림 + Issue 코멘트
- **실행은 수동**: Claude Code 세션에서 직접 `/dev-cycle` 실행
- **API 키 불필요**: ANTHROPIC_API_KEY 없이 동작

## 확장 계획

1. `handoff:pm→research` 트리거 추가
2. `handoff:dev→pm` 리뷰 요청 자동화
3. Self-hosted Runner로 완전 자동화 (선택적)
